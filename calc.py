"""A tiny module we'll use as a practice target for the git/PR/CI workflow.

The functions here are intentionally trivial — the point of this repo isn't
the code, it's practicing the *process* around changing code: branch, commit,
push, pull request, review, merge, and having CI check it automatically.
"""


def add(a, b):
    return a + b


def subtract(a, b):
    return a - b
