# git-practice

A throwaway repo for practicing the branch → commit → push → pull request →
review → merge → CI loop. The code (`calc.py`) doesn't matter — it's just
something small enough to make one-line changes to.
